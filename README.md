# Структура репозитория

```bash
├───apb_uart
│   ├───cmake
│   ├───rtl
│   │   ├───sim.sv -  Top модуль. Здесь подключаются: renode, apb_uart, uart_requester и другие модули
│   │	├───apb_uart.sv
│   │	├───uart_interrupt.sv
│   │	├───uart_rx.sv
│   │	├───uart_rx.sv
│   │	└───io_generic_fifo.sv
│   └───sim
├───common
│	├───irq.c irq.h - обработка прерываний.
│	├───sc_print.c sc_print.h - реализация ввода/вывода в консоль
│	└───mem.h - обращения к памяти.
├───renode
│   ├───src
│       ├───Plugins
│           ├───VerilatorPlugin
│               └───VerilatorIntegrationLibrary
│                   ├───hdl
│                   │   ├───renode.sv - Обработка входящих запросов по DPI
│                   │   ├───imports
│                   │   │   └───renode_pkg.sv - Реализация DPI функций для взаимодействия Renode и Verilator.
│                   │   └───modules
│                   │       ├───apb3
│                   │       ├───axi
│                   │       ├───uart
│                   │       │   └───uart_requester.sv - Реализация коннектора DPI для UART, этот модуль предстоит реализовать!
│                   │       └──renode_interrupts.sv - Передача сигналов прерываний между Renode и Verilator.
│                   └───src
│                       ├───buses
│                       ├───communication
│                       └───peripherals
│
│
│
├───tests - Директория с тестами UART
│   └───apb_uart_example
│   	├───app.c - код теста
│	└───Makefile - мейкфайл теста
└───Makefile - Корневой мейкфайл проекта с таргетами для сборки и запуска тестов





!!! Торопились, поэтому в некоторых словах отсутсвуют буквы из-за того, что иx нет на сервере( !!!

TEST_COVERAGE.IMG:
    Приведена картинка только локальных тестов, потому что не успели доделать DPI.

REQUESTER:
    В renode_requester были попытки реализовать свои dpi функции, для возвращения/считывания данных, а так же инициализация uart-шины(функция uart_init). Позже эти функции и их использование отсутствует, однако uart_init ранее работал, но в процессе работы перестал. В текущей версии uart_requester есть обработчики rx_i и tx_o, а так же есть обработчики для шины, через которую с помощью событий происходит коммуникация с датчиком. Логически все должно работать, однако не было времени на отладку и недостаточные знания SystemVerilog вынудили нас совсем небольшую часть кода для демонстрации работы с датчиком - отправки команды "а" и прием ответа. Это можно посмотреть в waveform. С этим небольшим отчетом в корне репозитория находится несколько скриншотов.

TESTS: FAILS(maybe bugs?)
    test_iir_all
    test_perm


```
